/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // Add external domains if you're using external images
  },
};

export default nextConfig;
